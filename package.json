{
  "name": "moi-radio-station-requests",
  "version": "1.0.0",
  "description": "Radio Station Request System - MOI Kuwait",
  "private": true,
  "scripts": {
    "install": "npm --prefix frontend install frontend",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend start"
  }
}
